const path = require('path');

// TODO: These paths need to come from config rather than being hardcoded
export const mapMyRunInputDir = '';
export const mapMyRunOutputDir = path.join('/home/dave/elasticsearch/data/csv.to.object');